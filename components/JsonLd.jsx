/* Inyecta datos estructurados JSON-LD (schema.org moderno, no microdata). */
export default function JsonLd({ data }) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  )
}
